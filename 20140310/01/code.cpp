#include<cstdio>
#include<string>

#define MAX_LEN 20

struct Point {
	int x, y;
};
int data_matrix[MAX_LEN][MAX_LEN];
char buf[MAX_LEN + 10];

Point points[MAX_LEN * MAX_LEN];


int main() {
	freopen("in.txt", "r", stdin);
	freopen("out.txt", "w", stdout);

	string str;
	int i = 0, j = 0;
	while(getline(str) != EOF {
		
	}

}
